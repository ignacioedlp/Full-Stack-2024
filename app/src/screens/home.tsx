import React from 'react';
import ThemeView from '@/components/screen';
import Typography from '@/components/typography';

const Home = () => {
  return (
    <ThemeView>
      <Typography variant="h1" text="Home" />
    </ThemeView>
  );
};

export default Home;
